{
  "buildCommand": "pnpm install && cd apps/dashboard && pnpm build",
  "outputDirectory": "apps/dashboard/dist",
  "installCommand": "pnpm install --filter dashboard",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
